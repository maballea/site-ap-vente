<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Créer une Nouvelle Catégorie</title>
    <!-- Ajout de Bootstrap CSS via CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>
    <div class="container mt-5">
        <!-- Titre de la page -->
        <h1 class="text-center mb-4 text-primary">Créer une Nouvelle Catégorie</h1>

        <!-- Formulaire de création de catégorie -->
        <div class="card shadow-sm p-4 mb-4">
            {{ include('categorie/_form.html.twig') }}
        </div>

        <!-- Lien retour à la liste des catégories -->
        <div class="text-center mt-4">
            <a href="{{ path('categorie_index') }}" class="btn btn-secondary btn-lg">
                <i class="bi bi-arrow-left-circle"></i> Retour à la liste
            </a>
        </div>
    </div>

    <!-- Ajout de Bootstrap JS via CDN pour les composants interactifs -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
